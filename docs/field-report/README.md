# Field report

Open `index.html` directly in a browser. It is a dependency-free interactive report so it remains
inspectable locally, on GitHub, and in a future Pages deployment without adding a frontend toolchain.

The report is a navigation layer, not the source of truth. Claims must link to retained evidence,
current code, or an authoritative project record. Update it whenever readiness changes materially.
