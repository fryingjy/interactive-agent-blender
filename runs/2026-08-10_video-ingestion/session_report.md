# Legal local video-ingestion validation

**Status:** PASS for ingestion mechanics; not an external curriculum lesson.

## Source truth

`project_owned_modeling_lesson.mp4` was generated by this project as a six-second instructional fixture. It is legally local and contains three original visual cards, an AAC audio tone, and a WebVTT sidecar transcript. It does not represent knowledge acquired from an outside educator.

## Observed modalities

- Video stream: yes, 640×360 at 6 fps, 6 seconds.
- Audio stream: yes; stream presence was detected, but no speech-recognition claim is made.
- Captions: yes, local WebVTT sidecar.
- Transcript: yes, three timestamped segments parsed from the sidecar.
- Frames: six timestamped PNGs decoded at 0, 1, 2, 3, 4, and 5 seconds.

The 3-second frame was visually inspected and correctly showed “STEP 2 — Inspect the evaluated surface.”

## Safety boundary

The ingester accepts only files inside explicitly approved local roots. It has no URL downloader, no account automation, and no platform-restriction bypass. Web ingestion separately requires an explicit hostname allowlist and rejects redirects outside it.

## Evidence

- `ingested/video_ingest_report.json`
- `ingested/frame_*.png`
- `project_owned_modeling_lesson.mp4`
- `project_owned_modeling_lesson.vtt`
- `knowledge_engine/ingest/video_ingest.py`
- `knowledge_engine/ingest/transcript_ingest.py`

## Limitation

This proves actual frame/audio/caption access and timestamp extraction. External modeling lessons remain unstudied until the user supplies a permitted file or an accessible permitted source.
